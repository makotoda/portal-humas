# Aset biner & dokumen tak boleh ikut ke Apps Script (GAS hanya menerima .js/.html/.json).
# bg-landing.jpg: sumber asli wallpaper beranda, sudah ditanam sbg data URI base64
# langsung di .bg (index.html). File ini cuma referensi, tak perlu ikut ke GAS.
**/*.jpg
**/*.md
docs/**
.git/**
.github/**
.claude/**
